test:
  name: "Use case scenario"
  description: "Default branching of the use case scenario"
  workers:
    number: 10
  rounds:
  - label: init
    txNumber: 500
    # txDuration: 
    rateControl:
      type: maximum-rate
      opt:
        tps: 100
        step: 5
        sampleInterval: 20
        includeFailed: true
    workload:
      module: ./init.js
      # arguments: 