version: '3'

services:
 caliper:
  container_name: caliper
  image: hyperledger/caliper:0.5.0
  command: launch manager
  environment:
  - CALIPER_BIND_SUT=fabric:2.5
  - CALIPER_BENCHCONFIG=benchmark.yaml
  - CALIPER_NETWORKCONFIG=network.yaml
  volumes:
  - ./:/hyperledger/caliper/workspace
